#pragma once
// Legacy compatibility header — include DebugRenderer2D.h directly for new code.
#include "Core/Debug/DebugRenderer2D.h"

// Backward-compat alias
using CDebugRenderer = CDebugRenderer2D;
