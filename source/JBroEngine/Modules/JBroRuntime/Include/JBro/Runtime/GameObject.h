#pragma once

namespace JBro::Engine
{
    // Runtime object representation is intentionally deferred until the
    // engine-side entity/component ownership model is established.
    class GameObject
    {
    };
}
