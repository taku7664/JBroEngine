# 2D 물리 계획 (기존 엔진 이식과 오목 폴리곤 결함 수정)

> 계약은 `docs/ProjectRule.md`, 결정은 `tasks/todo.md` Decisions 다. 이 문서는 그 둘을 향해 가는 순서와
> 상태를 적는다. 상태는 항목마다 `[완료]` `[진행]` `[제안]` `[가정]` `[열림]` 으로 붙인다.
> `[제안]` 은 **사용자 확인 전**이다. 2026-09-25 사용자 지시("기존 엔진을 기반으로 Physics 시스템 및 모듈 구현,
> 오목 폴리곤 버그 유의해서 이식 준비")로 시작했다. 같은 날 §5 의 여섯 가지가 모두 권고대로 확인돼 **D-199** 가 됐다
> (훅은 "차후 더 늘린다", 질의 확장은 "후순위지만 구현한다"). 같은 날 1~3 단계(커널의 기하·판정·월드와 솔버)가 섰다(§4).
> 4 단계(어댑터)는 스크립트 훅을 부르는 길을 D-207 으로 정한 뒤 섰다 - 엔진의 `Physics2DSystem` 이 이제 이 커널로 돈다.
> 남은 것은 5 단계(에디터)와 6 단계(질의 확장)다.

## 0. 한 줄 요약

기존 엔진의 물리(`Engine/GameFramework/Physics2D`, 약 4,900 줄)는 순차 임펄스·워밍스타트·서브스텝·SAP 브로드페이즈·
조인트·질의까지 갖췄지만, 오목 폴리곤에서 **법선 방향을 도형 중심으로 정했고**(오목 도형의 중심은 도형 밖에 있을 수 있다),
**조각으로 판정한 뒤 접촉점은 통짜 오목 도형으로 다시 만들었다.** 새 엔진에서는 캔버스를 모르는 물리 커널을 따로 두고,
오목 폴리곤은 **볼록 조각 하나하나를 독립된 자식 도형으로** 다룬다. 법선은 조각의 면에서만 나오고, 도형 중심으로 뒤집지 않는다.

## 1. 기존 엔진의 물리 - 무엇이 있었고 무엇이 아팠나

위치는 `C:\Users\박주형\source\repos\JBroEngine\Engine\GameFramework` 기준이다. 줄 번호는 2026-09-25 에 읽은 판이다.

### 1.1 있던 것

| 부분 | 파일 | 내용 |
|---|---|---|
| 컴포넌트 | `Component/Physics2DComponents.h` | `Rigidbody2D`(Static·Kinematic·Dynamic, 질량·관성·마찰·반발·감쇠·중력 배율·축 고정), `PolygonCollider2D`(정다각형 절차 생성 또는 직접 편집한 `LocalPoints`, 레이어·마스크, 트리거), `CircleCollider2D` |
| 조인트 | `Component/Physics2DJoints.h` | `DistanceJoint2D`(막대·로프·스프링), `HingeJoint2D`(점 구속). 접촉과 같은 속도 반복 안에서 푼다 |
| 한 스텝 | `Physics2DSystem.cpp:2623` | 적분 → 월드 기하 갱신 → 브로드페이즈(x 축 SAP, y 로 미리 거름) → 좁은 판정 → 같은 쌍 병합 → 워밍스타트 → 속도 반복 8 → 위치 반복 2 → 정지 안정화. 고정 스텝을 4 개 서브스텝으로 나눈다 |
| 좁은 판정 | `:385` `:693` `:1038` | 볼록 SAT, 원-다각형 SAT, 기준면·입사면 클리핑으로 접촉점 2 개, `FeatureId` 로 다음 스텝의 누적 임펄스를 되살림 |
| 오목 | `:1136~1308` | 귀 자르기(ear clipping)로 삼각형 조각을 만들고, 각 변이 원래 외곽 변인지(`BoundaryEdges`) 표시한다 |
| 이벤트 | `:1572` | 쌍의 Enter·Stay·Exit 를 직전 스텝과 비교해 스크립트 훅으로 보낸다(충돌·트리거 따로) |
| 질의 | `Physics2DSystem.h:140~175`, `Physics2DQueryGeometry.cpp` | Raycast·RaycastAll·OverlapPoint·OverlapCircle·OverlapBox(회전)·CircleCast·BoxCast, 레이어 마스크 |
| 할당 | 헤더 `:261~373` | 스크래치를 멤버로 두고 `clear()` 로 용량을 다시 쓴다. 병합 표는 오픈 어드레싱 |

잘 한 것으로 이어받을 것: 서브스텝 + 워밍스타트, 조인트를 접촉과 같은 반복에서 푸는 것, 브로드페이즈 쌍을 인덱스 순으로
다시 정렬해 결과가 배치 순서에 흔들리지 않게 한 것(헤더 `:320~324`), 프레임 경로의 스크래치 재사용, 질의가 충돌 판정과
같은 분기를 쓰게 맞춘 것(`:2035`).

### 1.2 오목 폴리곤이 틀렸던 이유

사용자가 보고한 증상은 "오목 폴리곤(안쪽이 들어간 모양)이 있으면 제대로 동작하지 않는다" 이다. 코드를 읽어 원인이 되는
자리를 여섯 개 찾았다. 앞의 셋은 아래 예시로 숫자까지 확인했고(awk 로 다시 계산, 2026-09-25), 나머지는 코드로만 확인했다.

**예시 도형 U** (반시계): `(0,0) (3,0) (3,3) (2,3) (2,1) (1,1) (1,3) (0,3)`. 3x3 정사각형에서 가운데 위쪽 1x2 가 파였다.
넓이는 7, 면적 가중 중심은 `(1.5, 1.357)` 이다. **이 점은 파인 홈 안(x 1~2, y 1~3), 즉 도형 밖에 있다.**
꼭짓점 산술 평균은 `(1.5, 1.75)` 로 역시 홈 안이다.

1. **법선을 두 도형 중심의 방향으로 뒤집는다** - `OrientNormal`(`:130`)을 `CalculateCentroid(a->WorldPoints)` 로 부른다
   (`:3149~3151`, 원은 `:3278~3279`). 볼록 도형에서는 맞지만 오목 도형에서는 틀린다.
   예: 한 변 0.6 인 상자 B 가 홈 안에서 왼쪽 안벽(x = 1)에 0.02 박혀 있고 중심이 `(1.28, 2.0)` 이다. 맞는 법선(A→B)은 `+x` 다.
   `cB - cA = (-0.22, 0.643)` 이고 `+x` 와의 내적이 -0.22 라서 **`-x` 로 뒤집힌다.** 솔버는 상자를 벽 속(왼쪽 기둥)으로 민다.
   홈 안에서 벽에 붙은 물체가 벽을 뚫거나 떨리는 증상과 맞는다.
2. **접촉점을 통짜 오목 도형으로 다시 만든다** - 법선과 침투는 조각에서 구하고, 접촉점은
   `BuildPolygonManifold(a->WorldPoints, b->WorldPoints, ...)`(`:3160`)로 **오목 도형 전체**에서 뽑는다. 기준면 클리핑은 볼록
   도형을 전제로 한다.
3. **오목 도형의 면 바깥 방향을 중심 쪽으로 판정한다** - `GetFaceOutwardNormal`(`:1028`)은 "변 가운데에서 중심을 향하지 않는
   쪽" 을 바깥으로 본다. U 의 왼쪽 안벽 변 `(1,1)→(1,3)` 은 참 바깥이 `+x` 인데, 산술 중심이 `(1.5, 1.75)` 라서 `-x` 로 판정된다.
   2 번과 겹쳐 기준면·입사면을 잘못 고른다. (같은 파일의 `GetPolygonOutwardNormal`(`:274`)은 부호 있는 넓이로 정하므로 맞다.
   맞는 함수와 틀린 함수가 한 파일에 같이 있었다.)
4. **관성과 넓이를 절댓값으로 더한다** - `CalculatePolygonInertia`(`:834`)가 `std::abs(Cross(p0, p1))` 를 더한다. 원점에서 편
   부채꼴 삼각형의 부호를 버리므로 오목 도형(그리고 원점이 도형 밖인 도형)에서 틀린다. U 에서 부호 있는 합은 14(넓이 7)인데
   절댓값 합은 22(넓이 11)라서 **넓이를 57% 크게** 잰다. 또 관성은 원점 기준인데 솔버의 회전 중심은 면적 중심(`:922`)이다.
   계수도 틀렸다. `(mass / 6) · Σ / area` 는 참값 `(mass / 12A) · Σ` 의 두 배라, 볼록한 단위 정사각형도 `m/6` 이 아니라
   `m/3` 이 된다(손으로 푼 값, 새 커널 테스트가 `m/6` 을 잰다).
5. **회전 중심과 적분 중심이 다르다** - 임펄스는 면적 중심을 팔 길이의 기준으로 쓰는데(`:3570`), 적분은 트랜스폼 원점을
   속도만큼 옮기고 원점을 기준으로 돌린다(`:888~902`). 면적 중심이 원점에서 먼 도형(L·U 자)일수록 한 스텝마다 중심이 어긋난다.
   각속도 상한(`:2718~2726`)의 주석이 "잘못된 회전 중심이 만든 각 임펄스 누적" 을 막는다고 적고 있다 - 원인 대신 증상을 막았다.
6. **한 쌍의 조각 결과를 하나로 줄인다** - 조각마다 SAT 를 돌린 뒤 **침투가 가장 얕은 조각 하나**만 남기고(`:3084`),
   이어서 같은 오브젝트 쌍의 매니폴드를 "가장 깊은 것의 법선" 하나로 합친다(`:3303~3374`). 물체가 두 조각에 걸쳐 박히면
   한쪽 침투만 풀린다. 오목 도형의 안쪽 모서리(L 의 꺾인 자리)에 원이 끼면 두 벽의 법선이 모두 필요한데 하나만 남는다.

그 밖에 코드로 보인 것:

- 주석과 코드가 다르다. `:3008` 은 "BoundaryEdge 필터링 없음" 이라고 적었지만 `:3082` `:3107` `:3134` 는 `BoundaryEdges` 를
  넘기고, `TestPolygonSAT`(`:433`)는 경계 변이 없으면 충돌을 버린다.
- `[가정]` 귀 자르기가 일직선 꼭짓점에서 멈출 수 있다. 일직선 꼭짓점은 귀 후보에서 빠지고(`:1236`), 삼각형 안 판정이 경계를
  포함하므로(`:1163`) 그 점이 다른 귀의 변 위에 있으면 그 귀도 막힌다. 귀를 못 찾으면 남은 부분을 버리고 나온다(`:1263`) -
  **도형 일부에 충돌이 없는 구멍**이 생긴다. 기존 코드를 돌려 보지는 않았다. 다만 새 구현의 첫 판이 비슷한 방식(허용 오차 안의
  일직선 점을 삼각형 없이 빼기)으로 넓이 0.03% 를 잃었고, 무작위 별 200 개 테스트와 덮임 검사가 그것을 잡았다(§4 의 1).
- 조각이 하나도 안 나오면 오목 도형 통짜를 조각 하나로 쓴다(`:1300~1307`). 오목 도형에 SAT 를 돌리는 것이라 틀린다.
- 볼록 여부를 매 스텝 월드 점으로 다시 재고(`:2990`), 임계값 아래의 얕은 홈은 볼록으로 본다(`:239`).
- 파생 자료(조각·월드 점·해시·더러움 표시)가 컴포넌트에 들어 있었다(`Physics2DComponents.h:451~474`).

### 1.3 기존 기록

기존 리포의 `tasks/PhysicsQueryAndTagFind.md` 는 "질의가 충돌 판정과 같은 분기를 쓴다" 와 "오목 폴리곤 대상 질의 vs 실제
충돌 일치" 를 완료로 적었다. **질의와 충돌이 서로 같다는 것만 확인했고, 충돌 자체가 맞는지는 재지 않았다.** 위 1~3 은 두 쪽이
똑같이 틀려도 그 검사를 통과한다.

## 2. 이식 전 새 엔진에 있던 것 (2026-09-25)

- `Component::Rigidbody2D`(`bodyType`·속도·`mass`·`gravityScale`·`linearDamping`·`fixedRotation`),
  `Component::Collider2D`(`shape` = Box·Circle·Capsule·Polygon, `offset`·`size`·`radius`·`isTrigger`) -
  `JBroFramework2D/Include/JBro/Framework2D/Component/Physics2D.h`. **폴리곤 꼭짓점 필드가 없다**(todo.md:156).
- `System::Physics2DSystem`(`JBroFramework2DSystem`, 243 줄): 중력과 감쇠를 적분해 `Transform2D` 를 옮길 뿐이다. **충돌이 없다.**
  Raycast·OverlapBox 는 Box·Circle 만 본다.
- `Service::Physics2DService`(Tier S) → `IPhysics2DSystem` 로 스크립트가 질의한다(D-24·D-42·D-43).
- `GameScript2D::OnCollisionEnter/Exit` 훅은 있지만 **부르는 곳이 없다.**
- 에디터 캔버스 뷰가 콜라이더 모양을 그린다(D-143). 폴리곤은 그리지 않는다.
- 3D 는 `Rigidbody3D`·`Collider3D` 골격뿐이다(todo-3d). 이 계획의 범위 밖이다.

## 3. 설계 (D-199)

### 3.1 계층

```
Tier S  JBroFramework2D      Rigidbody2D · Collider2D (저작 값만) · Collision2D · Physics2DService
           ▲
Tier E  JBroPhysics2D (새)   캔버스를 모르는 커널: World · Body · Shape(자식 조각) · 브로드페이즈 ·
           ▲                  좁은 판정 · 솔버 · 질의 · 기하(검증·분해·질량)
Tier E  JBroFramework2DSystem  Physics2DSystem = 어댑터: 컴포넌트 → 커널 동기화, 스텝, 트랜스폼 되쓰기,
                               이벤트 발송, IPhysics2DSystem 질의 전달
```

- **커널을 따로 두는 이유.** (1) 오목 도형의 정답을 캔버스·트랜스폼 없이 숫자로 잴 수 있어야 한다 - 기존 엔진이 놓친 것이
  정확히 그 검사다(§1.3). (2) 기존 규모가 4,900 줄이라 어댑터와 섞으면 `Physics2DSystem.cpp` 가 다시 한 파일에 몰린다.
  (3) 네트워크 코어를 캔버스와 떼어 둔 선례가 있다(D-122). DLL 경계는 두지 않는다(정적 링크, ProjectRule §3).
- 커널은 `JBroCore` 와, `Vec2`·`Matrix3x2` 때문에 `JBroFramework2D` 의 `Math2D.h` 만 쓴다. 컴포넌트·`Canvas`·`GameObject` 는
  모른다. 이것은 의존하지 않은 헤더를 include 하면 컴파일이 실패하는 음성 테스트로 지킨다.
- 커널의 식별자는 번호다. 바디와 도형은 커널 배열의 `index + generation` 핸들이고, 어댑터가 컴포넌트의 `InstanceId` 와 짝을
  들고 있다. `SafePtr` 는 커널에 들어가지 않는다.

### 3.2 도형과 오목 폴리곤

- **컴포넌트에는 저작 값만 둔다.** `Collider2D` 에 `points`(`Array<Vec2>`, Polygon 일 때만 쓰임)를 더한다. 조각·월드 점·해시는
  커널의 도형이 들고, 꼭짓점 글자가 바뀌면(해시 비교) 다시 만든다. 기존처럼 컴포넌트에 파생 자료를 넣지 않는다(§1.2 끝).
- **검증과 정리를 먼저 한다.** 분해하기 전에 (1) 거의 같은 점 합치기, (2) 일직선 점 빼기, (3) 부호 있는 넓이로 반시계 정규화,
  (4) 자기 교차 검사를 한다. 자기 교차이거나 넓이가 0 이면 도형을 만들지 않고 로그를 남긴다(값은 지우지 않는다). 구멍은 받지 않는다.
- **분해는 귀 자르기 + Hertel-Mehlhorn 병합**이다. 삼각형을 만든 뒤 대각선을 지워도 볼록이 유지되면 이웃 조각을 합친다.
  조각은 볼록 다각형이고 꼭짓점이 `MaxPolygonVertices`(8, Box2D 와 같은 값) 이하가 되도록 자른다. 볼록한 입력은 분해하지 않는다.
  분해 결과는 **조각 넓이의 합 = 원래 넓이**로 검사한다(§1.2 의 구멍 가정을 잡는다).
- **조각 하나가 곧 자식 도형이다.** 좁은 판정은 (도형 A, 조각 i) × (도형 B, 조각 j) 마다 독립이고 매니폴드도 따로다. 법선은
  그 조각의 SAT 에서 나오며 **도형 중심으로 뒤집지 않는다.** 부호는 "기준면이 속한 쪽 → 상대" 로 정한다. 기준면·입사면의 바깥
  방향은 조각의 감긴 방향으로 정한다(중심을 쓰지 않는다). §1.2 의 1·2·3·6 이 여기서 사라진다.
- **안쪽 이음매(조각 사이 대각선).** 볼록 조각의 모든 변은 그 조각에 대해 맞는 분리 축이라 SAT 는 필터 없이 돌린다. 물체가
  이음매를 가로질러 박히면 이음매 법선이 나올 수 있다(흔히 "유령 충돌"). 서브스텝으로 침투가 얕아서 드물다. 정적 지형에서
  문제가 되면 뒤에 체인(선분 모음) 도형을 따로 둔다 - 이번 범위 밖 `[열림]`.
- **질량.** 조각마다 부호 있는 넓이·면적 중심·관성을 구해 합하고, 관성은 평행축 정리로 바디의 질량 중심 기준으로 옮긴다.
  원은 `πr²`. 바디의 `mass` 필드는 그대로 두고 밀도 = mass / 전체 넓이로 나눈다. §1.2 의 4 가 여기서 사라진다.
- 캡슐(`ColliderShape2D::Capsule`)은 "선분 + 반지름" 으로 원·볼록과 같은 SAT 경로를 쓴다. 기존 엔진에는 없었다. 4 단계 뒤로 미룬다.

### 3.3 바디와 적분

- 커널 바디는 **질량 중심의 위치와 각도**를 상태로 들고 적분한다. 트랜스폼 원점은 `원점 = 중심 - R(각도) · 로컬 중심` 으로
  되쓴다. 임펄스의 팔 길이도 같은 중심을 쓴다. §1.2 의 5 가 여기서 사라지고, 각속도 상한은 안전망으로만 남긴다.
- 부모가 있는 오브젝트의 바디는 기존처럼 월드 변위를 부모 로컬로 바꿔 되쓴다(`:313`). 부모가 움직이는 동적 바디는 `[열림]`.
- 한 오브젝트의 콜라이더 여럿이 한 바디의 도형이 된다(`Collider2D` 는 `Multiple` 이다, todo.md:3077).

### 3.4 솔버 (기존 것을 이어받는다)

- 서브스텝 4, 속도 반복 8, 위치 반복 2, Baumgarte 위치 보정, 정지 안정화(기존 기본값). 순차 임펄스 + 워밍스타트.
- 누적 임펄스를 이어 주는 열쇠는 **(도형 A 핸들, 조각 i, 도형 B 핸들, 조각 j, FeatureId)** 다. 기존 열쇠는 오브젝트 주소 쌍이었다.
  열쇠가 정수라 표 비교에 문자열이 없다.
- 브로드페이즈는 기존 SAP 를 조각 단위 AABB 로 옮긴다. 쌍을 (A, B) 순으로 다시 정렬해 결과를 결정적으로 둔다.
- 스크래치는 커널 멤버 `Array` 로 두고 `Clear` 로 용량을 다시 쓴다(프레임 경로 힙 할당 금지).

### 3.5 어댑터 (`Physics2DSystem`)

- `OnFixedUpdate` 에서 (1) 활성 `Rigidbody2D`·`Collider2D` 를 커널에 맞춘다(새로 생김·사라짐·값 바뀜), (2) 스텝, (3) 커널 자세를
  `Transform2D` 와 `linearVelocity`·`angularVelocity` 에 되쓴다, (4) 이벤트를 보낸다.
- 바디가 없는 콜라이더는 정적 도형이다(기존과 같다).
- 이벤트: 오브젝트 쌍으로 모아 Enter·Exit 를 판정한다(조각 여럿에 닿아도 오브젝트 쌍의 이벤트는 하나). 판정만 모으고
  솔버의 매니폴드는 합치지 않는다. 트리거 훅과 Stay 는 §5 의 결정을 따른다.
- 에디터는 멈춘 채로 뜨므로(D-131) 재생 전에는 스텝하지 않는다. 재생을 멈추면 커널을 비운다(기존 `OnSimulationStop` 의 역할).

### 3.6 질의

- 지금의 `Raycast`·`OverlapBox` 를 커널의 조각 단위 판정으로 옮겨 폴리곤·캡슐도 맞게 한다. 질의와 충돌이 같은 조각 목록을 쓴다.
- 기존 질의 중 `RaycastAll`·`OverlapPoint`·`OverlapCircle`·`CircleCast`·`BoxCast`·레이어 마스크는 서비스 표면을 넓히는 일이라
  §5 의 결정을 따른다.

## 4. 단계 (D-199)

각 단계는 테스트가 먼저이고, 단계마다 커밋한다. 뮤테이션은 기존 관례대로 단계 끝에 잰다.

1. `[완료]` **커널 뼈대와 기하.** `JBroPhysics2D` 모듈(빌드 단위, 음성 테스트), 폴리곤 검증·정리, 분해(귀 자르기 + 병합), 질량 속성.
   - 완료 조건: U·L·별·일직선 점이 섞인 도형·중복 점이 섞인 도형에서 조각 넓이의 합이 원래 넓이와 같다. 조각이 모두 볼록이고
     8 점 이하다. U 의 넓이 7·면적 중심 `(1.5, 1.357)`·관성이 해석값과 같다. 자기 교차 도형은 거절된다.
   - 선 것: `Geometry.h` 의 `CleanPolygon`(일직선·중복 점 빼기 → **자기 교차 검사를 넓이보다 먼저**(8 자 모양은 넓이가 0 일 수
     있다) → 반시계로)·`DecomposePolygon`·`ComputePolygonMass`·`ComputeOutlineMass`(외곽선에서 바로 구하는 두 번째 계산)·
     `CombineMass`. 테스트 `Physics2DGeometryTests`: U·L 해석값, 조각 질량 = 외곽선 질량, 무작위 별 200 개(넓이·관성·**덮임**:
     격자점마다 조각이 정확히 한 겹인지 - 넓이 합만으로는 도형 밖으로 삐져나간 조각을 못 잡는다), 병합이 U 3·L 2·20각형 3 의
     최소 조각에 이름, 시계 방향 입력, 나비·꼭짓점에서 맞닿은 도형·두 점·일직선·같은 점 거절. 음성 테스트: 커널이 컴포넌트·
     `Canvas`·`GameObject` 헤더를 넣으면 셋 다 C1083.
   - 실측: 첫 판은 분해 도중 허용 오차 안의 일직선 점을 삼각형 없이 빼서 무작위 별 하나에서 넓이 0.0017(0.03%)을 잃었다 -
     분해 안에서는 정확히 넓이 0 인 점만 빼고 볼록 판정은 부호로만 한다. 뮤테이션 14/15 잡힘. 살아남은 하나(중복 점 제거 단계를
     지움)는 일직선 제거가 같은 점을 빼므로 동치였고, 그 단계를 코드에서 뺐다.
2. `[완료]` **좁은 판정과 브로드페이즈.** 원-원, 원-볼록, 볼록-볼록(SAT + 클리핑, FeatureId), 조각 단위 SAP.
   - 완료 조건: §1.2 예시의 상자(U 의 홈 안, 왼쪽 안벽)에 대해 법선이 `+x` 다. L 의 안쪽 모서리에 낀 원이 매니폴드 둘을 받는다.
     접촉점이 모두 해당 조각의 경계 위에 있다.
   - 선 것: `Collision.h`(`Pose`·`Circle`·`Manifold`, 미리 만드는 접촉 거리 `SpeculativeDistance` = 0.02), `BroadPhase.h`
     (`SweepAndPrune`). 법선은 언제나 A→B 이고 기준면에서만 나온다. 테스트 `Physics2DCollisionTests`: U 의 왼쪽·오른쪽 안벽과
     홈 바닥, 순서를 바꾸면 법선만 뒤집힘, B 가 기준면을 내는 경우(돌린 상자의 모서리), 넓은 면이 좁은 기준면으로 잘림, L 의 안쪽
     모서리의 원이 벽마다 매니폴드, 원의 모서리 영역과 중심이 안에 든 경우, 미리 만드는 접촉, 접촉 번호 유지, 경계, 브로드페이즈 =
     모든 쌍 비교(순서까지, 맞닿은 상자 포함).
   - 실측: 첫 판의 테스트는 모두 두 쪽 깊이가 같아 **B 가 기준면인 갈래를 한 번도 지나지 않았다** - 뮤테이션 목록을 짜다 보여 더했다.
     뮤테이션 17/19 잡힘. 살아남은 둘은 동치다: B 축 조기 종료를 지워도 점마다 거르는 검사가 모든 점을 버리고(B 의 축이 가르면
     모든 점이 그만큼 떨어져 있다), 원 중심이 도형 안이면 가장 얕은 변 위에 투영이 떨어져 꼭짓점 영역이 생기지 않는다.
3. `[완료]` **솔버.** 적분(질량 중심), 워밍스타트, 서브스텝, 위치 보정, 정지 안정화.
   - 완료 조건: U 의 홈에 떨어뜨린 상자가 바닥에 멈추고 벽을 넘지 않는다. 정적 U 위에서 옆으로 민 상자가 홈 안벽에서 멈춘다.
     L 자 동적 바디가 제 질량 중심을 기준으로 돌고, 외력 없이 자유 회전할 때 중심이 움직이지 않는다. 상자 10 개 쌓기가 3 초 안에 멈춘다.
     같은 입력을 두 번 돌리면 결과가 비트까지 같다.
   - 선 것: `World.h` - 바디·도형은 index + generation, 도형 하나가 조각 여럿을 자식으로 든다. 서브스텝(4)마다 속도 적분 →
     판정(조각 단위, 열쇠 (도형, 조각, 도형, 조각) 로 **다시 정렬한 뒤** 직전 접촉과 맞대어 누적 임펄스를 이어받음) → 워밍스타트 →
     속도 반복(8, 마찰 먼저, 떨어진 점은 틈만큼만 다가오게) → 반발 → 위치 적분(질량 중심) → NGS 위치 보정(3, 앵커 변위로 현재 깊이를
     되짚고 LinearSlop 만큼 남김, 속도를 더하지 않음) → 원점 되짚기. 이벤트는 도형 쌍마다 하나로 줄이고(조각 여럿이어도), 지워진
     도형의 접촉은 다음 스텝에 끝으로 나오며 userData 를 싣는다. 트리거는 무게가 없고 밀지 않는다. 레이어·마스크, 키네마틱.
   - 실측: 뮤테이션을 스크래치 빌드에서 먼저 재 13/17 이었다. 살아남은 넷이 가리킨 빈자리: (1) 미리 만드는 접촉 때문에 떨어뜨린
     물체는 박히지 않아 **위치 보정이 한 번도 일하지 않았다** → 겹쳐 생긴 상자가 튀지 않고 빠지는 장면을 더함, (2) 모든 장면에서
     접촉이 이미 열쇠 순이라 정렬을 지워도 같았다 → 가장 먼저 만든 공을 스택 꼭대기에 얹어 순서가 깨지게 함, (3)·(4) 트리거의 무게와
     감쇠를 재지 않았다. L 자 테스트는 "원점이 충분히 움직였다" 만 봐서 원점 = 중심이 되는 변이가 살았다 - 원점의 정확한 자리를 잰다.
     보강 뒤 17/17. 같은 목록을 워크트리의 전체 테스트 실행기로 다시 재도 17/17 이다(`tools/mutations-physics3.txt`).
4. `[완료]` **어댑터와 이벤트.** 동기화, 되쓰기, Enter·Exit 발송, 질의 이전. 캔버스 파일 왕복에 `points` 가 남는다.
   - 완료 조건: 캔버스를 세운 통합 테스트에서 스크립트가 `OnCollisionEnter` 를 한 번, 떨어진 뒤 `OnCollisionExit` 를 한 번 받는다.
     오목 도형 두 조각에 동시에 닿아도 Enter 는 한 번이다. 재생을 멈추고 다시 켜도 남은 Exit 가 튀지 않는다.
   - 선 것(네 커밋): (4a) `GameScript2D::OnTriggerEnter/Exit`, 2D 스크립트 등록은 `RegisterScriptType2D<T>` 로만 하고 `GameScriptBase`
     에서 바로 파생한 타입은 컴파일 시간에 거절(C2338 음성 검사), `Framework2DServiceContextAbiVersion` 2 (D-207).
     (4b) `Collider2D` 에 `points`·`friction`·`restitution`·`layer`·`mask` - 내장 컴포넌트의 첫 배열 필드라 속성 테스트의 잎사귀
     걷기가 배열 원소로 내려가게 고쳤고, 캔버스 파일 왕복을 잰다. (4c) 커널의 시작 이벤트가 가장 깊은 접촉점과 A→B 법선을 싣고,
     조각·원 단위 반직선과 겹침 판정을 더했다. (4d) `Physics2DSystem` 이 고정 스텝마다 활성 `Rigidbody2D`·`Collider2D` 를 커널에
     맞춘다: 오브젝트마다 바디 하나(켜진 Rigidbody2D 가 없으면 정적), 트랜스폼의 크기는 도형에 곱하고(음수는 뒤집기), 모양이나
     성질이 바뀌면(지문 비교) 도형·바디를 다시 만든다. 스텝 뒤 움직이는 몸의 자세·속도를 부모 로컬로 되쓰고, 지난번에 쓴 값과
     다르면 스크립트가 옮긴 것으로 보고 순간 이동한다. 정적인 몸은 옮긴 자리로 따라간다. 시작·끝을 두 오브젝트의 모든
     `GameScript2D` 에 각자 쪽 법선으로 보내고(발송 내내 파괴를 미룸), 트리거는 트리거 훅만 부른다. 끄거나 지운 콜라이더는 같은
     스텝에 끝을 낸다. 질의는 여전히 지금의 컴포넌트를 보고, 돌린 상자와 오목 폴리곤을 맞게 잰다. 커널 타입은 공개 헤더에 나오지
     않는다(시스템이 불투명한 `State` 로 든다) - 에디터·호스트는 커널 include 경로 없이 선다.
   - 테스트 `Physics2DSystemTests`: 떨어진 상자의 착지와 양쪽 훅(상대·법선·몸 종류·접촉점), 순간 이동과 끝, U 의 두 기둥에 얹힌
     뚜껑과 홈에 떨어진 조약돌(시작 한 번, 안벽 안), 트리거, 콜라이더 끄기·다시 켜기·상대 파괴, 멈췄다 다시 켜기, 질의(U 의 홈 바닥,
     돌린 상자의 모서리, 홈 안 겹침 없음, 옮긴 직후), 정적인 몸 따라가기, 크기, L 자의 질량 중심 회전(원점 = 2·중심).
     기존 중력 테스트는 서브스텝 적분의 값(-g·h²·10)으로 고쳤다 - 한 번에 옮기던 옛 값보다 참값에 가깝다.
   - 실측: 목록을 짜다 **정적인 몸을 옮기는 장면과 떨어지는 도중의 속도 되쓰기를 재지 않은 것**이 보여 먼저 테스트를 더했다.
     어댑터 뮤테이션(`tools/mutations-physics4.txt`) 첫 판 15/17. 살아남은 둘: 콜라이더를 끄는 장면이 모두 바디째 사라지는 쪽이라
     "도형을 지우지 않기" 가 드러나지 않았고, 월드 캐시 검사는 캐시가 처음부터 거짓이라 저절로 참이었다 - Rigidbody2D 가 있는 상자의
     콜라이더를 끄는 장면과 캐시를 참으로 둔 뒤의 검사를 더해 17/17.
   - 남긴 것 `[열림]`: (1) 크기가 매 스텝 바뀌면(크기 애니메이션) 도형을 매 스텝 다시 만들어 끝·시작 이벤트가 되풀이된다 -
     커널에 도형 바꾸기(접촉 유지)가 필요하다. (2) 캡슐은 충돌하지 않는다(§4 의 7). (3) 부모가 회전과 비균등 크기를 함께 가지면
     되쓰기가 찌그러짐을 무시한다. (4) 부모에 Transform 이 있는데 꺼져 있으면 그 아래 콜라이더는 물리에서 빠진다(ProjectRule 의
     규칙대로다. 옛 질의 코드는 그런 부모를 루트로 보았다). (5) 실제 에디터에서 재생해 본 확인은 아직 없다.
5. **에디터.** 인스펙터의 `points` 편집(커맨드), 캔버스 뷰의 폴리곤과 조각 그리기, 거절된 폴리곤 표시.
   꼭짓점 끌기 도구(기존 엔진의 캔버스 뷰 폴리곤 편집)는 기존 화면을 먼저 읽고 따로 계획한다.
6. **질의 확장** (D-199 (6), 후순위지만 구현한다): `RaycastAll`·`OverlapPoint`·`OverlapCircle`·`CircleCast`·`BoxCast`·레이어 마스크.
   기존 엔진 `Physics2DQueryGeometry.cpp` 의 스윕 판정을 커널의 조각 단위로 옮긴다. 서비스·`IPhysics2DSystem` 표면이 넓어진다.
   - 완료 조건: 질의마다 원·볼록·오목(U 의 홈을 지나가는 레이·원 스윕) 대상 결과가 해석값과 같다.
7. **뒤의 것** `[열림]`: 조인트(Distance·Hinge, 새 컴포넌트), 캡슐, Stay 등 훅 추가(D-199 (5) "차후 더 늘린다"), 체인 도형, 연속 충돌(CCD).

## 5. 결정 (2026-09-25 확인, D-199)

1. 새 모듈 `JBroPhysics2D`(Tier E 커널)를 둔다(§3.1).
2. `Collider2D` 에 `points` 필드를 더한다. 도형별 컴포넌트로 나누지 않는다.
3. 오목 도형은 볼록 조각을 자식 도형으로 다룬다(§3.2).
4. 마찰·반발·레이어·마스크는 넷 다 `Collider2D` 필드다.
5. 스크립트 훅은 `OnTriggerEnter/Exit` 둘을 더하고 Stay 는 두지 않는다. 훅은 차후 더 늘린다.
6. 질의 확장은 4 단계에 넣지 않고 6 단계로 둔다 - 후순위지만 구현한다.

## 6. 열린 것과 가정 모음

- `[완료]` 4 단계의 스크립트 훅 발송 경로 - D-207. 2D 프로젝트의 스크립트는 모두 `GameScript2D` 이고 등록이 그것을 컴파일
  시간에 막으므로, 물리는 `static_cast` 로 부른다.
- `[가정]` 귀 자르기가 일직선 점에서 구멍을 만든다(§1.2) - 기존 코드는 돌려 보지 않았다.
- `[열림]` 부모가 움직이는 동적 바디, 조각 이음매의 유령 충돌, 체인 도형, CCD, 3D 물리.
- `[열림]` 질량 중심이 트랜스폼 원점과 다를 때 에디터가 원점을 기준으로 두는 기즈모와 물리의 회전 중심이 다르게 보인다.
  표시만의 문제라 5 단계에서 판단한다.
