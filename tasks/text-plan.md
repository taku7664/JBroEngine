# 2D 텍스트 계획 (기존 엔진 Text2D 이식과 구조 재검토)

> 계약은 `docs/ProjectRule.md`, 결정은 `tasks/todo.md` Decisions 다. 이 문서는 그 둘을 향해 가는 순서와
> 상태를 적는다. 상태는 항목마다 `[완료]` `[진행]` `[제안]` `[가정]` `[열림]` 으로 붙인다.
> `[제안]` 은 **사용자 확인 전**이다. 2026-09-25 사용자 지시("텍스트 계획서 초안 써 줘, 그리고 이 구조가 최선이었는지도
> 깨트려 봐")로 시작했다. 같은 날 §6 의 여섯 가지가 모두 권고대로 확인돼("전부 권고대로 해") **D-200** 이 됐다.
> 1 단계(커널)가 섰다(2026-09-25, `2ef2001`·`d8434da`). 2 단계(아틀라스·`Font` 에셋·`TextStore`·`Text2D`·`Text2DSystem`)가 섰다
> (2026-09-26, `bd6e043`). 저장소의 자리는 D-206 이 고쳤다. 다음은 3 단계(에디터)다.

## 0. 한 줄 요약

기존 엔진의 `Text2D` 는 FreeType(SDF) + HarfBuzz 로 한글·라틴을 그렸지만, **HarfBuzz 를 글자 묶음 하나씩 불러서 커닝이
한 번도 적용되지 않았고**, 텍스트가 바뀔 때마다 GPU 버퍼를 새로 만들었으며, 64 px SDF 한 벌로 외곽선 폭의 상한이 글자 크기에
묶여 있었다. 그리고 새 엔진에서는 그 모양을 그대로 가져올 수 없다: **D-51 이 컴포넌트 공개 필드에 `String` 을 금지한다.**
권고는 (1) 캔버스를 모르는 텍스트 커널 모듈을 두고, (2) 글자를 **스프라이트 인스턴스로** 제출해 지금 렌더러를 그대로 쓰며,
(3) 문자열은 컴포넌트 밖의 호스트 소유 저장소에 두고 핸들과 세대로 가리키는 것이다. 1 단계는 비트맵(커버리지) 글자라서
렌더러를 고치지 않고도 선다. SDF 와 외곽선은 4 단계다.

## 1. 기존 엔진의 텍스트 - 무엇이 있었고 무엇이 아팠나

위치는 `C:\Users\박주형\source\repos\JBroEngine` 기준이다. 줄 번호는 2026-09-25 에 읽은 판이다.

### 1.1 있던 것

| 부분 | 파일 | 내용 |
|---|---|---|
| 설계 문서 | `Docs/Text2DDesign.md` (64 줄) | 범위(UTF-8 한글·라틴, 월드 공간), 폰트 탐색 순서, 공개 필드, 검증 기준, 제외 범위(리치 텍스트·캐럿·양쪽 정렬·내장 폰트·컬러 이모지·MSDF·비동기 생성) |
| 컴포넌트 | `Engine/GameFramework/Component/Text2D.h` | `String Text`, `FontFamilyGuid`, `FontStyle`, 크기·폭·높이(픽셀), `OverflowMode { Overflow, Wrap, Clip }`, 자동 크기(Min~Max), 가로·세로 정렬, 줄 간격·자간, 채우기·외곽선 색과 폭, `PixelSnap` |
| 폰트 자산 | `Engine/Core/Asset/FontAsset.h` | `FontFaceAsset`(TTF/OTF 바이트·face 번호·세대), `FontFamilyAsset`(Regular·Bold·Italic·BoldItalic face, 패밀리 폴백 목록, 프로젝트 폴백 사용 여부) |
| 시스템 | `Engine/GameFramework/Rendering/TextRenderSystem.cpp` (679 줄) | FreeType 로 64 px SDF 를 뜨고, HarfBuzz 로 글리프 번호와 전진 폭을 구하고, face 마다 1024² RGBA8 아틀라스 페이지를 선반 할당으로 채우고, 텍스트마다 메시를 만들어 페이지별 렌더 아이템으로 제출한다 |
| 셰이더 | `Engine/Core/Renderer/Forward2DRenderer.cpp:249~259` | SDF 한 번 샘플로 채우기와 외곽선 커버리지를 한 드로에서 합성한다(반투명 겹침이 생기지 않는다) |

잘 한 것으로 이어받을 것:

- 채우기와 외곽선을 **한 드로에서 합성**한 것. 외곽선을 따로 그려 겹치면 반투명 글자의 겹친 자리가 진해진다.
- 레이아웃 결과를 RHI 와 떼어 둔 CPU 자료로 본 것(설계 문서 "레이아웃과 캐시").
- 한 프레임 안에서 (패밀리, 스타일) 해석 결과를 메모해 텍스트 수가 아니라 패밀리 수만큼만 해석한 것(`TextRenderSystem.h:190~198`).
- 재료(머티리얼) 캐시 키를 문자열에서 정수 쌍으로 바꾼 것(`TextRenderSystem.h:168~181`). 문자열 키가 프레임마다 힙을 탔다는 실패담이 주석에 남아 있다.
- 폰트가 없을 때 그리지 않고 오브젝트마다 한 번만 경고한 것.
- 한글 자모와 결합 문자를 앞 글자와 함께 셰이핑해야 한다는 것을 안 것(`:319~330`).

### 1.2 깨트려 본 결과 - 코드에서 확인한 결함

"이 구조가 최선이었나" 에 대한 답이다. 코드로 확인한 것과 계산으로만 추정한 것을 나눠 적는다.

1. **HarfBuzz 를 글자 묶음 하나씩 불러서 커닝과 합자가 적용되지 않는다.** (코드로 확인)
   `BuildLayout`(`:317~345`)은 코드포인트 하나를 읽고, 뒤따르는 결합 문자·자모만 붙인 뒤, 그 조각만 `ShapeRun` 에 넘긴다.
   HarfBuzz 는 넘겨받은 조각 안에서만 GPOS 커닝과 GSUB 합자를 적용하므로, `AV`·`To` 처럼 **두 글자 사이의 조정은 한 번도
   일어나지 않는다.** 결과적으로 HarfBuzz 는 "코드포인트 → 글리프 번호 + 전진 폭" 이상의 일을 하지 않았다. 큰 의존성을 들이고
   그 값어치를 쓰지 못한 자리다.
2. **줄바꿈이 단어가 아니라 글자 묶음 단위다.** (코드로 확인)
   `:337` 이 글자 묶음마다 폭을 넘는지 보고 바로 줄을 바꾼다. 영어 `hello` 가 `hel` / `lo` 로 갈린다. 공백에서 끊는 규칙이 없다.
   한국어는 글자 단위 줄바꿈도 흔히 쓰이지만, 어절 단위를 고를 수 없다.
3. **텍스트가 바뀔 때마다 GPU 버퍼를 새로 만든다.** (코드로 확인)
   `RebuildText`(`:432~535`)가 `Immutable` 정점·인덱스 버퍼를 새로 만든다(`:410~422`). 점수·타이머처럼 매 프레임 바뀌는 텍스트는
   프레임마다 버퍼 생성·파기가 일어나고, 그 전에 `std::string` 정규화 사본(`:285~299`), `std::vector` 여럿, `unordered_map` 하나를
   힙에 만든다. ProjectRule 의 "매 프레임 경로에 힙 할당 금지" 에 걸린다. 자동 크기는 이진 탐색 8 번이 모두 레이아웃을 새로 한다.
4. **변경 감지가 매 프레임 문자열 전체를 해시한다.** (코드로 확인)
   `OnUpdate` 가 텍스트마다 `BuildSignature`(`:424`)를 부르고, 그 첫 줄이 `std::hash<std::string>{}(text.Text)` 다. 바뀌지 않은
   텍스트도 매 프레임 글자 수만큼 돈다.
5. **캐시 키가 컴포넌트 주소다.** (코드로 확인)
   `m_textCache` 가 `const void*` 를 키로 쓴다(`:618`). 풀에서 파기된 자리에 새 컴포넌트가 오면 옛 캐시를 이어받는다. 시그니처가
   다르면 다시 만들어서 대개 드러나지 않지만, 같은 글자를 가진 새 오브젝트는 옛 메시와 경계를 그대로 받는다.
6. **렌더 시스템이 컴포넌트에 경계를 되써 준다.** (코드로 확인)
   `Text2D.h:18~23` 이 이것을 "경계 규약의 유일한 예외" 라고 적었다. 에디터의 클릭 선택과 외곽선이 렌더 시스템이 한 번 돈 뒤에야
   맞는 값을 본다. 생성 직후 프레임과 폰트가 없는 동안에는 빈 경계다.
7. **외곽선 폭의 상한이 글자 크기에 묶이고, 넘으면 사각형이 칠해진다.** (셰이더 식으로 확인, 수치는 추정)
   셰이더는 `outerThreshold = 0.5 - fwidth(d) * width` 다(`Forward2DRenderer.cpp:253`). `width` 가 **화면 픽셀**이라, 카메라를
   멀리 빼면 글자에 비해 외곽선이 굵어진다. 또 SDF 는 64 px 로 한 번만 뜨고 FreeType SDF 의 기본 퍼짐은 8 px 이다(`[가정]` 기본값,
   `EnsureGlyph` 가 퍼짐을 설정하지 않는다). 32 px 글자를 1:1 로 보면 표현 가능한 외곽선은 약 8 × 32/64 = 4 px 인데 입력은
   64 까지 받는다(`TextRenderSystem.cpp:661`). 문턱이 0 아래로 가면 쿼드 전체가 외곽선 색이 되어 **글자마다 사각형이 보인다.**
8. **64 px SDF 아틀라스는 한국어에서 페이지가 빨리 늘어난다.** (추정)
   음절 하나가 64 px 에서 대략 56~60 px 이고 퍼짐 8 px 가 양쪽에 붙으면 약 73 x 77 칸이다. 1024² 한 장에 약 14 x 13 ≈ 180 자가 들어간다.
   대사가 있는 한국어 게임은 서로 다른 음절 수백 ~ 천여 개를 쓰므로 face 하나에 페이지가 3~8 장 생기고, 한 문자열이 여러 페이지를
   쓰면 페이지마다 드로가 나뉜다. 페이지마다 RGBA8 이라 4 MB 이고, 실제로 쓰는 채널은 하나다.
9. **폰트 패밀리 자산과 폴백 체인이 1 판 범위에 비해 무겁다.** (설계 판단)
   리치 텍스트가 제외 범위인데도 `FontStyle` 과 스타일별 face, 패밀리 안의 폴백 목록, 프로젝트 폴백, 순환 방문 검사(`AppendFamilyFaces`)까지
   있었다. 한 텍스트가 한 스타일만 쓴다면 "Bold face 를 직접 고른다" 로 같은 결과가 나온다. 패밀리 자산은 리치 텍스트(한 문자열 안에서
   굵게 전환)가 들어올 때 비로소 값어치가 생긴다.
10. **글리프 생성이 메인 스레드에서 동기로 돈다.** (코드로 확인, 비용은 추정)
    FreeType 의 SDF 래스터라이저는 비트맵보다 훨씬 비싸다. 처음 보는 음절이 많은 대사 한 줄이 한 프레임에 몰리면 끊긴다.
    제외 범위에 "비동기 glyph 생성" 이 있었으므로 알고 미룬 것이다.

### 1.3 기존 기록

기존 엔진 `todo.txt`·작업 노트에서 텍스트의 실패담은 따로 찾지 못했다. 위의 항목은 모두 이번에 코드를 읽어 찾은 것이다.
`Docs/Text2DDesign.md` 의 검증 기준(한글 음절·자모, 잘못된 UTF-8, 폴백 순서, CRLF, Overflow·Wrap·Clip, 자동 크기 경계,
아틀라스 다중 페이지, 폰트 재로드, 저장·재로드, 선택)은 그대로 이어받을 만하다.

## 2. 지금 새 엔진에 있는 것

- **게임 쪽 텍스트는 없다.** 에디터 글자는 ImGui 가 자기 아틀라스(내장 `imstb_truetype.h` 1.26)로 그린다.
  `JBroCanvas/CanvasFile.h:12` 는 기존 씬 파일의 `Text2D` 를 읽지 않는다고 적었다.
- **2D 렌더 경로**: 시스템이 `SpriteRenderItem`(`RenderWorld2D.h:26`)을 제출하고, `RenderWorld2D::Sort` 가 16 B 키
  `[layerOrder | renderOrder | 예약 16 비트]` 로 정렬하며, 키가 같으면 `sourceId` 로 안정화한다. 브리지가 `SpriteSubmit` 으로 옮기고
  렌더러가 단위 쿼드를 인스턴스로 그린다(`GpuSpriteInstance` 40 B). 셰이더는 `texture * tint` 이고 블렌드는 알파다.
- **텍스처**: `Renderer::RegisterTexture` 는 RGBA8 전체를 받고, `UpdateTexture` 는 **같은 크기 전체**를 갈아 끼운다.
  RHI `WriteTexture` 는 **GPU 가 끝날 때까지 기다리고 프레임 안에서는 거절한다**(`RHI.h:441~448`). 부분 영역 쓰기가 없다.
  `TextureFormat` 에 단일 채널(R8)이 없다.
- **프레임 순서**: `EngineInstance` 가 `m_framework->Update` 를 부른 뒤 `m_renderer->BeginFrame` 을 연다(`EngineInstance.cpp:592·623`).
  시스템의 Update 는 렌더러 프레임 밖이므로 거기서 텍스처를 올릴 수 있다.
- **샘플러**: 프로젝트 기본이 `TextureFilter: Nearest`(D-119) 다. SDF 는 Linear 로 읽어야 한다.
- **D-51**: 컴포넌트 공개 필드에 `String` 을 두지 않는다. 이름·태그는 `NameId` 로 인턴하고 문자열은 에디터·직렬화 계층이 든다.

## 3. 구조를 깨트려 본 갈림길

기존 모양을 기본값으로 두지 않고, 갈림길마다 반례를 먼저 세웠다. 각 항목 끝의 권고는 2026-09-25 에 모두 확인돼 D-200 이 됐다.

### 3.1 문자열은 어디에 사나 (가장 먼저 풀어야 한다)

기존의 `String Text` 는 D-51 에 막힌다. 막는 이유가 실제로 있다: 스크립트 DLL 이 `text = "..."` 로 대입하면 DLL 쪽 할당기로
메모리를 잡고, 호스트가 풀에서 파기할 때 호스트 쪽 할당기로 푼다(`Allocator.h:40~44` 가 "다른 모듈이 만든 저장소를 이 모듈의
연산으로 해제하면 안 된다" 고 적었다).

- (a) **호스트 소유 저장소 + 핸들.** 컴포넌트에는 `TextId`(번호 + 세대, POD)만 둔다. 글자는 텍스트 시스템의 저장소가 들고,
  스크립트는 `Text2DService::SetText(handle, const char* utf8, std::uint32_t length)` 로 바꾼다(기존 설계 문서도 DLL 경계의 문자열
  API 를 `(const char*, length)` 입력으로 적었다). 저장소가 쓸 때마다 세대를 올리므로 **변경 감지가 정수 비교 하나**가 되어 §1.2 의
  4 번이 사라진다. 에디터는 커맨드로 같은 저장소를 고치고, 캔버스 파일에는 글자로 적는다.
- (b) **컴포넌트 안의 고정 버퍼**(`char text[256]`). POD 라 단순하지만 길이가 묶이고 컴포넌트가 256 B 커진다. 대사 한 문단이 넘친다.
- (c) **D-51 에 예외를 둔다.** 가장 싸지만 결정 하나를 되돌리는 일이고, 할당기 불일치는 CRT 설정에 따라 조용히 숨는다.

권고 `[결정 D-200]`: (a). 뒤에 로컬라이징이 오면 같은 자리에 "키로 가리키는 글자" 를 더할 수 있다(`textKey: NameId`).

### 3.2 누가 무엇을 하나 - 한 덩어리 렌더 시스템인가

기존은 폰트 로드·셰이핑·레이아웃·아틀라스·메시·재료·제출·경계 되쓰기가 `CTextRenderSystem` 하나에 있었다. 반례: 레이아웃이 맞는지
재려면 캔버스와 RHI 를 세워야 했다. 물리(D-199)와 네트워크(D-122)는 같은 이유로 캔버스를 모르는 커널을 따로 두었다.

권고 `[결정 D-200]`: 커널 모듈 `JBroText`(Tier E)를 두고 **폰트 face·셰이핑·줄바꿈·레이아웃·아틀라스 채우기(CPU 픽셀)** 를 거기에 둔다.
입력은 UTF-8 과 레이아웃 옵션, 출력은 POD 글리프 배열(위치·크기·아틀라스 칸·페이지)과 경계다. 캔버스·컴포넌트·렌더러를 모른다.
Framework2DSystem 의 `Text2DSystem` 은 어댑터로서 저장소 세대 비교 → 커널 레이아웃 → 아틀라스 텍스처 올리기 → 스프라이트 아이템 제출만 한다.
경계는 시스템의 캐시(키는 `InstanceId`)가 들고 에디터가 그것을 묻는다. **컴포넌트에 되쓰지 않는다**(§1.2 의 5·6 번).

### 3.3 텍스트마다 메시인가, 글자마다 인스턴스인가

기존은 텍스트마다 정점 버퍼를 만들었다. 새 렌더러는 이미 **단위 쿼드 인스턴싱**이라, 글자 하나를 `SpriteRenderItem` 하나로 보내면
UV 사각형(D-113)과 틴트와 정렬이 그대로 쓰인다. GPU 버퍼를 만들 일이 없다.

반례로 세운 것과 답:
- **정렬 안정성**: 한 텍스트의 글자들은 `sourceId` 가 같아 서로의 순서가 정해지지 않는다. 겹치지 않는 채우기 글자는 상관없지만
  4 단계의 외곽선이 이웃 글자에 겹칠 수 있다. 정렬 키의 예약 16 비트(`[15:0]`)에 글자 순번을 넣으면 된다(65,536 자까지).
- **제출 용량**: `RenderWorld2D` 는 예약 용량을 넘으면 떨어뜨린다(`SubmitSprite`). 글자 2,000 자짜리 텍스트는 스프라이트 예산을
  그만큼 먹는다. 용량 산정에 텍스트 글자 수를 더해야 한다 `[열림]`.
- **인스턴스 크기**: 40 B × 글자 수. 1 만 자여도 400 KB 라 문제가 아니다.

권고 `[결정 D-200]`: 글자마다 스프라이트 인스턴스. 4 단계에서 SDF 셰이더가 필요해지면 `SpriteSubmit` 에 파이프라인 종류와 외곽선
값을 더한다(§4.5). 텍스트용 제출 목록을 따로 두면 같은 레이어 안에서 스프라이트와 텍스트를 섞어 정렬할 수 없으므로 두지 않는다.

### 3.4 FreeType + HarfBuzz 인가, stb_truetype 인가

§1.2 의 1 번대로라면 기존 엔진은 HarfBuzz 의 값어치(GPOS 커닝·GSUB 합자·복잡한 문자 체계)를 쓰지 않았다. 범위가 한글·라틴이면:

| | FreeType 2.13 + HarfBuzz 12 | stb_truetype 1.26 (단일 헤더) |
|---|---|---|
| 크기 | 소스 수십만 줄, 빌드 단위 둘 | 헤더 하나, 5,000 줄 남짓. ImGui 가 이미 사본을 빌드한다 |
| TTF·OTF(CFF) | 둘 다 | 둘 다(`imstb_truetype.h:1304·1417`) |
| 커닝 | GPOS 전부(HarfBuzz) | `kern` 표 + GPOS 쌍 조정(1.19 부터, `:2501`). 클래스 쌍까지 |
| 합자·결합 문자 위치(GPOS mark) | 된다 | 안 된다 |
| 힌팅(작은 글자 선명도) | 된다 | 없다 |
| SDF | `FT_RENDER_MODE_SDF` | `stbtt_GetGlyphSDF`(`:953`) |
| 복잡한 문자 체계(아랍·인도계) | 된다 | 안 된다 |

반례로 세운 것과 답:
- **분해된 한글(NFD, macOS 파일 이름 등)**: 자모 조합은 GSUB 없이도 산술로 된다(`0xAC00 + (L×21 + V)×28 + T`). 입력을 음절로
  정규화하면 stb 로도 맞게 나온다. 옛한글·조합 불가 자모는 못 한다.
- **라틴 결합 문자(`e` + U+0301)**: stb 는 위치를 못 잡는다. 흔한 조합은 NFC 로 미리 합칠 수 있지만 표가 필요하다. 입력이 대부분
  미리 합쳐진 형태라 드문 경우다.
- **작은 글자**: 힌팅이 없으면 12~14 px 한글이 흐리다. 2D 게임 UI 에서 실제로 보이는 차이다. 픽셀 폰트(비트맵 폰트 파일)는 힌팅과 무관하다.
- **나중에 바꾸는 비용**: 커널의 face·셰이핑 표면을 우리 타입으로 닫아 두면, 라이브러리를 바꿀 때 커널 안만 바뀐다.

권고 `[결정 D-200]`: **1 판은 stb_truetype.** `ThirdParty/stb/stb_truetype.h` 로 정식 사본을 넣는다(ImGui 의 `imstb_` 사본은 ImGui 의
것이라 가져다 쓰지 않는다, ThirdParty 규칙). 커널 표면을 라이브러리와 무관하게 닫아서, 작은 글자 품질이나 복잡한 문자 체계가
필요해지면 FreeType + HarfBuzz 로 커널 안만 바꾼다. 이 판단은 §6 의 질문 2 로 확인받는다.

### 3.5 SDF 한 벌인가, 모드를 고르나

프로젝트 기본 필터가 Nearest(D-119)라는 것은 이 엔진이 픽셀 아트 2D 를 기본으로 본다는 뜻이다. 픽셀 폰트에 SDF 를 쓰면
모서리가 둥글어지고, Linear 샘플링이 필요해 픽셀 격자가 흐려진다. 반대로 확대·축소가 잦은 HD 텍스트는 비트맵이면 크기마다 아틀라스를 새로 떠야 한다.

권고 `[결정 D-200]`: 폰트 에셋 임포트 옵션에 `renderMode { Bitmap, Sdf }` 를 둔다.
- `Bitmap`: (face, 픽셀 크기) 마다 커버리지 아틀라스. RGBA8 에 `(255, 255, 255, 커버리지)` 로 적으면 **지금 스프라이트 셰이더(`texture * tint`)
  가 그대로 그린다.** 필터는 에셋의 텍스처 필터를 따른다. 외곽선은 없다(1 판).
- `Sdf`: face 마다 한 벌. **뜨는 크기를 64 가 아니라 32~48 로** 두고 퍼짐을 임포트 옵션으로 연다. 외곽선 상한은 퍼짐이 정하므로
  입력 폭을 그 값으로 자르고, 폭 단위는 화면 픽셀이 아니라 **글자 픽셀**로 한다(§1.2 의 7 번). 전용 셰이더가 필요하다(4 단계).

### 3.6 아틀라스를 언제 어떻게 올리나

지금 RHI 는 전체 텍스처 쓰기만 되고 GPU 를 기다린다. 새 글자가 생길 때마다 1024² RGBA8(4 MB)을 올리고 멈추면, 대사를 한 글자씩
찍는 연출에서 음절마다 끊긴다.

- 1 단계: 시스템 Update(렌더러 프레임 밖)에서 **그 프레임에 새로 생긴 글자를 모아 한 번만** 올린다. 비용을 실측한다.
- 임포트 옵션 `prewarm { None, Ascii, Ksx1001 }`: KS X 1001 의 한글 2,350 자를 미리 채우면 대부분의 한국어 게임 텍스트가 런타임
  래스터화 없이 그려진다. 32 px 비트맵이면 대략 2,350 × 36² ≈ 3 M 픽셀이라 1024² 세 장쯤이다(추정). `.jpak` 익스포트가 서면 미리 뜬
  아틀라스를 패키지에 넣어 게임 실행이 폰트 래스터화를 하지 않게 할 수 있다 `[열림]`.
- 부분 영역 쓰기(`WriteTexture` 에 사각형)와 R8 포맷은 RHI 세 백엔드의 일이라 따로 둔다 `[열림]`. 필요하다는 실측이 나오면 연다.

### 3.7 패밀리·스타일·폴백

권고 `[결정 D-200]`: 1 판은 **`Font` 에셋 하나(= face 하나)** 만 둔다. `Text2D::fontId` 가 그것을 가리키고, 비어 있으면 프로젝트 기본 폰트다.
폴백은 **프로젝트 설정의 순서 있는 폰트 목록 하나**다(한글 폰트에 없는 이모지·기호를 다른 폰트에서 찾는 용도). 패밀리 자산, `FontStyle`,
패밀리 안 폴백, 순환 검사는 리치 텍스트가 들어올 때 다시 본다 `[열림]`.

### 3.8 셰이핑 단위와 줄바꿈

- 같은 폰트가 이어지는 **줄 안의 구간 전체**에 커닝을 적용한다(stb 는 이웃 글리프 쌍마다 `stbtt_GetGlyphKernAdvance`).
- 줄바꿈 기회: 공백 뒤, 한글·한자·가나 음절 사이, 명시적 개행. 폭을 넘으면 마지막 기회로 되돌아가고, 기회가 없는 긴 단어는 글자에서 끊는다.
  `wrapMode { Word, Character }` 에서 `Word` 는 한글도 어절 단위(공백)로만 끊는다. 기본값은 `Word` 다(D-200 (5), §6).
- CRLF·CR 은 LF 로 본다. 잘못된 UTF-8 은 U+FFFD 로 바꾼다(기존과 같다). 정규화 사본을 만들지 않고 읽으면서 처리한다.

## 4. 설계 (D-200)

### 4.1 계층

```
Tier S  JBroAssetTypes        Asset::Font (메타·임포트 옵션: renderMode · pixelSize · sdfSpread · pixelsPerUnit · prewarm)
Tier S  JBroFramework2D       Component::Text2D (저작 값 + TextId) · Service::Text2DService (SetText · GetText 길이/복사)
           ▲
Tier E  JBroText (새)         캔버스를 모르는 커널: FontFace(stb) · Shaper(커닝) · LineBreaker · Layout · GlyphAtlas(CPU 픽셀, 선반 할당)
           ▲
Tier E  JBroAsset             Font 로더(바이트 소유, ReloadInPlace 때 세대 증가)
Tier E  JBroFramework2DSystem Text2DSystem = 어댑터: 저장소 세대 비교 → 레이아웃 → 아틀라스 업로드 → 글자마다 SpriteRenderItem
                              TextStore(호스트 소유 UTF-8 저장소, TextId 발급)
```

- 커널은 `JBroCore` 만 쓴다. `Vec2` 가 필요하면 물리 커널(D-199)처럼 `Math2D.h` 만 본다. 의존하지 않은 헤더를 include 하면 실패하는
  음성 테스트로 지킨다. 3D 텍스트가 오면 같은 커널을 쓴다.
- 커널의 식별자는 번호다(face 번호, 아틀라스 페이지 번호). `SafePtr` 는 커널에 들어가지 않는다.

### 4.2 컴포넌트 `Text2D`

저작 값만 둔다. 이름은 기존을 따르되 현재 규칙(접두사 없음, 소문자 시작 필드)에 맞춘다.

| 필드 | 형 | 비고 |
|---|---|---|
| `text` | `TextId` | 저장소 핸들. 인스펙터는 여러 줄 입력 칸으로 보이고, 캔버스 파일에는 글자로 적힌다 |
| `fontId` | `AssetId` | 비면 프로젝트 기본. `Id` 로 끝나므로 에셋 해석 패스와 `AssetField` 가 그대로 붙는다(D-115·D-118) |
| `fontSize` | `float` | 글자 픽셀. 유닛 크기는 폰트 에셋 PPU 로 나눈다(D-119 와 같은 규칙) |
| `boxSize` | `Vec2` | 글자 픽셀. 0 이면 무제한 |
| `overflow` | `enum { Overflow, Wrap, Clip }` | 기존과 같다 |
| `wrapMode` | `enum { Word, Character }` | §3.8 |
| `alignX`·`alignY` | `enum` | Left·Center·Right / Top·Middle·Baseline·Bottom |
| `lineSpacing`·`letterSpacing` | `float` | |
| `color` | `Color` | 채우기 |
| `outlineColor`·`outlineWidth` | `Color`·`float` | Sdf 폰트에서만 쓰인다(4 단계). 폭은 글자 픽셀 |
| `renderOrder` | `int32` | `SpriteRenderer2D` 와 같은 뜻 |
| `pixelSnap` | `bool` | |

자동 크기(Min~Max)는 1 판에서 뺀다 `[열림]`. 필요해지면 캐시한 레이아웃의 줄 폭으로 이진 탐색을 하되, 탐색 중 할당이 없게 한다.

### 4.3 저장소와 변경 감지

- ~~`TextStore` 는 캔버스마다 하나이고 Framework2DSystem 이 든다~~ → **프로세스에 하나이고 `JBroRuntime` 이 든다(D-206)**. 코덱이 문맥을 받지
  않고, 캔버스가 시스템을 오브젝트보다 먼저 내리기 때문이다. 슬롯은 `{ String utf8, generation, revision }`, 핸들은 `번호 + 세대`.
- 컴포넌트가 붙을 때 슬롯을 받고 떨어질 때 돌려준다. 캔버스 파일을 읽을 때 글자를 슬롯에 넣는다. 복사·붙여넣기·프리팹은 글자를 복사해 새 슬롯을 받는다.
- 시스템 캐시(키 `InstanceId`)는 `(text 세대, 폰트 핸들 세대, 레이아웃 옵션 해시)` 가 같으면 레이아웃을 건너뛴다. 옵션 해시는 필드 몇 개의
  정수·실수 결합이라 글자 수와 무관하다.
- **글자가 바뀐 텍스트의 레이아웃은 매 프레임 할당 없이 돈다**: 캐시마다 글리프 배열 용량을 들고 `Clear` 로 다시 쓴다.

### 4.4 아틀라스

- `GlyphAtlas` 는 CPU 픽셀(RGBA8)과 선반 할당기와 "더러운 페이지" 표시를 든다. 키는 `(face, 픽셀 크기 또는 Sdf, 글리프 번호)`.
- 페이지는 1024² 로 시작한다. 가득 차면 새 페이지를 만들고 옛 칸을 옮기지 않는다(기존과 같다, UV 가 고정된다).
- 시스템은 레이아웃이 끝난 뒤 더러운 페이지만 `Renderer::UpdateTexture` 로 올린다(프레임 밖). 첫 판은 페이지 전체를 올린다.
- 비우기는 폰트가 재로드될 때(세대가 바뀔 때)만 한다. 오래 안 쓴 글자의 퇴출은 `[열림]`.

### 4.5 렌더러 (4 단계에서만 바뀐다)

- `SpriteSubmit` 과 `SpriteRenderItem` 에 `shading { Sprite, SdfText }` 와 `outlineColor`·`sdfParams(퍼짐 대비 문턱)` 를 더한다.
  렌더러는 같은 (파이프라인, 텍스처, 필터) 가 이어지는 구간을 한 드로로 묶는다. 인스턴스 레이아웃이 커지므로 스프라이트 전체가 영향을 받는다 -
  `GpuSpriteInstance` 를 둘로 나눌지(텍스트 전용 인스턴스 버퍼) 한 벌로 키울지는 4 단계 시작 때 실측으로 정한다 `[열림]`.
- SDF 셰이더는 기존 식을 이어받되, 폭을 `fwidth` 곱이 아니라 퍼짐 대비 비율(글자 픽셀)로 넣고 문턱을 0 이상으로 자른다.

### 4.6 에디터

- 인스펙터: `text` 는 여러 줄 글자 칸 위젯(새 `Widget::TextArea`, §11.1 대로 위젯 계층), 편집은 커맨드 하나(되살릴 값 = 옛 글자).
- 캔버스 뷰: 시스템 캐시의 경계로 선택·외곽선. 캐시가 없으면(폰트 없음) 경고 표시와 작은 기본 상자.
- 에셋 브라우저: `.ttf`·`.otf` 가 `Font` 로 등록되고, 인스펙터에 임포트 옵션이 뜬다(D-120 과 같은 모양).
- 폰트가 없으면 인스펙터에 지속 경고, 콘솔에 오브젝트마다 한 번 경고(기존과 같다).

### 4.7 스크립트

`Text2DService` 는 `Physics2DService` 처럼 `Framework2DServiceContext` 를 거친다(D-42·D-43). `SetText(Ref<Text2D>, const char* utf8, uint32 length)`,
`GetTextLength`, `CopyText(buffer, capacity)`. 입력은 POD 이고 호스트가 복사한다. 숫자 표시처럼 자주 바뀌는 것을 위해
`SetTextFormat` 류는 두지 않는다 - 스크립트가 자기 버퍼에 쓰고 `SetText` 를 부르면 된다.

## 5. 단계 (D-200)

각 단계는 테스트가 먼저이고, 단계마다 커밋한다. 뮤테이션은 기존 관례대로 단계 끝에 잰다.

1. **커널 뼈대와 레이아웃.** `JBroText` 모듈(빌드 단위, 음성 테스트), `stb_truetype.h` 추가, face 로드, 커닝, 줄바꿈, 정렬, 경계.
   - 완료 조건: 시험 폰트(라이선스가 맞는 OFL 폰트 하나를 리포에 넣는다. 테스트 자료 자리가 아직 없으므로 위치는 이 단계에서 정한다)로 `AV` 의 전진 폭 합이 커닝 없는 값보다 작다. `hello world` 가
     `hello` 뒤에서 끊긴다(`Word`). 한글 문장이 `Word` 에서는 공백에서, `Character` 에서는 음절 사이에서 끊긴다. 분해된 자모 `ᄒ ᅡ ᆫ` 이 `한` 의
     글리프 하나로 나온다. 잘못된 UTF-8 이 U+FFFD 다. CRLF 가 한 줄바꿈이다. 레이아웃을 같은 입력으로 두 번 돌리면 할당이 0 번이다(두 번째).
   - `[완료]` (2026-09-25, `2ef2001`·`d8434da`) 모듈 `Modules/JBroText`(`FontFace.h`·`TextLayout.h`), `ThirdParty/stb/stb_truetype.h` 1.26,
     시험 폰트 `Tests/Data/Fonts/NotoSansKR-Subset.otf`(Noto Sans KR 2.004 에서 ASCII + 한글 32 자만 남긴 19,224 바이트 OFL 서브셋,
     `Embed.ps1` 이 `Tests/TestFontNotoSansKR.generated.h` 로 넣는다 - 테스트가 실행 폴더로 파일을 찾지 않는다), `Tests/TextLayoutTests.cpp`.
     **정한 것**(계획서에 없던 자리):
     - 커널은 `JBroCore` 만 본다. 좌표가 `float` 이라 `Math2D` 도 필요 없다 - 3D 가 같은 커널을 쓸 때 걸릴 것이 없다.
     - `FontFace` 는 바이트를 복사해 들고 `stbtt_fontinfo` 를 불투명한 192 바이트 자리에 둔다(헤더에 stb 가 나오지 않는다). 옮길 수 있고 복사는 막았다.
       stb 구현은 `STBTT_STATIC` 으로 `FontFace.cpp` 안에 가둔다 - ImGui 의 사본도 static 이라 링크에서 만나지 않는다.
     - 치수는 em 기준이다(`fontSize` = em 픽셀, FreeType 의 `FT_Set_Pixel_Sizes` 와 같다). 줄 높이는 첫 번째 열린 face 의 hhea
       `ascent - descent + lineGap` 에 `lineSpacing` 을 곱한 것이다. 폴백 face 는 자기 em 으로 전진 폭을 잰다.
     - face 고르기: 앞에서부터 그 글자가 있는 face, 없으면 U+FFFD 가 있는 face, 그것도 없으면 첫 face 의 .notdef.
       커닝은 같은 face 가 이어질 때만 준다.
     - 잘못된 UTF-8: 끊긴 열은 이어지던 바이트까지 U+FFFD 하나(WHATWG 의 최대 부분열), 겹친 인코딩·서로게이트·범위 초과는 열 전체가 하나.
     - 줄바꿈 기회: 공백 뒤, 한자·가나 앞뒤(Word 에서도), `Character` 면 글자 사이 어디나. 공백은 줄 끝에 매달리고 폭에 들어가지 않는다.
       기회가 없으면 넘친 글자에서 끊고, 상자보다 넓은 글자 하나는 그 줄에 홀로 둔다(멈추지 않는다). 탭은 공백 하나다.
     - 정렬은 블록 기준이다: 가로 블록은 `boxWidth`(없으면 가장 긴 줄), 세로 블록은 `boxHeight`(없으면 줄 수 × 줄 높이). `AlignX`·`AlignY` 가
       블록의 어느 점이 원점인지 정하고 줄은 블록 안에서 같은 쪽에 붙는다. `Baseline` 은 첫 줄 기준선이 y = 0 이다.
     - `Clip` 은 내림까지 상자 아래로 넘는 줄을 통째로 버린다. 글리프를 반만 자르는 것은 아틀라스와 함께 2 단계다.
     **잰 것**: fontTools 로 서브셋을 따로 읽은 값(README 의 표)과 stb 가 읽은 값이 모두 같다 - hhea 1000·1160·-288·0, 전진 폭, 커닝 `AV`·`VA` -15,
     `To` -74(GPOS 부표 형식 2). 레이아웃을 em 1000 으로 돌려 픽셀이 곧 폰트 단위가 되게 하고 숫자를 그대로 대조했다.
     음성 검사 다섯이 C1083 이다: `JBroText` 소스에서 `JBro/Runtime/Component.h`·`JBro/Framework2D/Math2D.h`·`JBro/Canvas/Canvas.h`·
     `JBro/Graphics/Renderer.h`, 테스트에서 `stb_truetype.h`. 대조로 `JBro/Text/TextLayout.h` 만 넣은 것은 컴파일된다.
     전체 솔루션(`Debug_Game2D`), 에디터 호스트(`Debug`), 테스트 전부가 통과한다.
     **뮤테이션 18/18 잡힘**(`tools/mutations-text1.txt`, 손으로 돌려 변이마다 어느 검사가 죽는지 확인): 커닝 안 줌, face 가 커닝을 못 읽음,
     공백 뒤 기회 없음, `Character` 무시, 기회 대신 넘친 글자에서 끊음, 공백을 글리프로 냄, CRLF 를 둘로 셈, 끝소리 자모 안 합침,
     끊긴 열을 바이트마다 셈, 겹친 인코딩 받아들임, 자간 무시, 가운데 줄 안 옮김, 기준선을 위쪽으로, `Clip` 이 줄을 안 버림, 줄 높이에서 내림 빠뜨림,
     글리프 배열·글자 배열을 다시 할당함, 옮긴 뒤 원본이 열린 채. **처음 판에서는 "다시 할당" 이 운으로 잡혔다** - 테스트가 버퍼 주소를 비교했는데,
     풀었다 같은 크기로 다시 잡은 블록이 같은 주소로 올 수 있어 손으로 돌리니 살았다(`mutate.py` 는 그 판에 우연히 잡힘으로 셌다).
     `TextLayout::GetReservedCapacity`(안쪽 배열 넷의 용량 합)를 두고 그 값을 비교하게 고쳐 세 번씩 돌려 모두 잡혔다(`d8434da`).
     완료 조건의 "할당 0 번" 은 이 용량 비교로 잰다 - 할당 횟수를 세는 훅은 엔진에 없다(`BindHeapAllocator` 는 한 번만 묶을 수 있어 테스트에 쓰지 않았다).
2. ~~**아틀라스와 비트맵 렌더.** `GlyphAtlas`(선반·페이지·더러운 표시), `Font` 에셋과 로더, `TextStore`, `Text2D`, `Text2DSystem`, 글자마다 스프라이트 아이템.~~
   → 완료 2026-09-26 · `13ac0ef`·`bd6e043`·`3fe31bd` · `JBroText/GlyphAtlas`, `JBroAsset/Asset.cpp`(`ReadFont`), `JBroRuntime/TextStore`,
   `JBroFramework2D/Component/Text2D.h`·`Service/Text2DService`, `JBroFramework2DSystem/System/Text2DSystem`·`Rendering/TextLibrary` (D-204 형식)
   - 완료 조건: 픽셀 테스트에서 흰 배경에 검은 `A` 가 기대한 칸 안에만 찍힌다. 같은 텍스트를 매 프레임 바꾸는 동안 GPU 버퍼 생성이 0 번이다.
     새 글자가 없는 프레임에 텍스처 업로드가 0 번이다. 페이지가 넘치면 둘째 페이지가 생기고 첫 페이지의 UV 가 바뀌지 않는다. 폰트를 다시 쓰면 세대가 올라 다시 그려진다.
     캔버스 파일 왕복에 글자가 남는다. 스크립트가 `SetText` 로 바꾼 글자가 다음 프레임에 보인다.
   - 만든 것:
     - 커널: `GlyphAtlas`(face 하나의 CPU 아틀라스, RGBA8 `(255,255,255,커버리지)`, 선반 할당·1 px 틈, 칸은 옮기지 않고 차면 새 페이지,
       새 칸이 든 페이지만 더러움), `FontFace::MeasureGlyphBitmap`·`RasterizeGlyph`(stb 는 여전히 `FontFace.cpp` 안).
     - 에셋: `Font` 로드·재로드·수거(`FontData { options, bytes, dataGeneration }`), `.jmeta` 의 `Font.ImportOptions { pixelsPerUnit, filter }`.
       `filter: Default` 는 로드 때 프로젝트 필터가 되고 PPU 0 이하는 100 이다(텍스처와 같은 규칙, D-119). **렌더 모드 옵션은 두지 않았다** -
       쓰이지 않는 옵션을 먼저 만들지 않는다. 4 단계(SDF)에서 온다.
     - `TextStore`·`TextId`(JBroRuntime, 프로세스에 하나 - **D-206 이 D-200 (1) 의 "캔버스마다" 를 고쳤다**), 코덱의 이스케이프(D-206 (b)).
     - `Component::Text2D`(14 필드: `text` `fontId` `font` `fontSize` `boxSize` `overflow` `wrapMode` `alignX` `alignY` `lineSpacing`
       `letterSpacing` `color` `renderOrder` `visible`, 기존 엔진 표처럼 Rendering·여럿 붙음), `Service::Text2DService`(SetText·GetTextLength·
       CopyText) → `System::IText2DSystem`(호스트 코드). 2D 시스템 문맥·서비스 문맥 ABI 가 둘 다 2 가 됐다.
     - `System::Text2DSystem`(실행 순서 410) 이 `TextLibrary`(폰트 에셋 슬롯마다 face 사본·아틀라스·페이지 텍스처)를 소유한다. 한 프레임은
       (1) 판번호·폰트 세대·옵션 키가 바뀐 텍스트만 다시 레이아웃하고 새 글리프를 아틀라스에 넣고, (2) 더러운 페이지만 올리고, (3) 글리프마다
       `SpriteRenderItem`(피벗 {0,1}, 크기 = 칸 픽셀 / 폰트 PPU)을 낸다. 색·그리기 순서는 옵션 키에 들어가지 않아 레이아웃을 다시 하지 않는다.
       `Clip` 은 상자 밖의 글리프 사각형과 UV 를 줄인다. 캐시는 `InstanceId` 키이고 떼인 텍스트의 것은 그 프레임에 빠진다. 경계는
       `GetLocalBounds` 로 묻는다(컴포넌트에 되쓰지 않는다).
     **계획에서 바뀐 것**:
     - 커널의 `Clip` 은 줄의 **위쪽**이 상자 안이면 남긴다(1 단계는 내림까지 들어가야 남겼다). 상자보다 큰 한 줄이 통째로 사라지지 않고
       잘려 보이게 하려는 것이다. 걸친 조각은 시스템이 자른다.
     - 글자 크기는 정수 픽셀로 반올림해 레이아웃과 래스터화를 같은 크기로 한다(비트맵과 글자 사이가 어긋나지 않게).
     - 폰트가 없는 텍스트는 그리지 않고 컴포넌트마다 한 번 경고한다. **프로젝트 기본 폰트와 폴백 목록은 아직 없다**(아래 `[열림]`).
     **잰 것**(테스트, D3D12 한 벌, 64 x 64 창, 카메라 2 유닛 = 64 px, 폰트 PPU 32 라 글자 픽셀 = 화면 픽셀):
     - 흰 바탕의 검은 40 px `A` 가 커널로 따로 잰 칸 안에만 찍힌다(±1 px).
     - 바뀌지 않은 프레임 둘: 레이아웃 0 번, 업로드 0 번, 같은 픽셀. 스크립트 서비스로 `V` 로 바꾸면 레이아웃 1 번·업로드 1 번, `A` 로
       되돌리면 업로드 0 번(이미 아는 글리프)이고 픽셀이 처음과 같다. 그동안 렌더러 텍스처 등록 수가 그대로다(GPU 버퍼 생성 0 번).
     - 색만 바꾸면 레이아웃 0 번, 크기를 바꾸면 1 번. `Clip` 상자(16 x 40)가 `A` 의 양옆과 아래를 자른다.
     - 300 px 음절 30 개가 둘째 페이지를 연다. 첫 페이지의 `A` 는 같은 픽셀로 그려진다. (**200 px 음절 30 개는 1024² 한 장에 들어갔다** -
       §1.2 의 8 번과 §3.6 의 칸 크기 추정은 실제보다 컸다. 페이지당 글자 수는 계산보다 많다.)
     - 폰트 메타의 PPU 를 32 → 64 로 고쳐 제자리 재로드하면 다시 레이아웃되고 절반 크기로 그려진다.
     - 까다로운 글자 21 개(여러 줄, `a: b`, 앞뒤 공백, 따옴표 두 종류, `[]`·`{}`, 백슬래시, 탭, CRLF, 한글 등)가 진짜 캔버스 파일을 지나
       그대로 돌아오고, 스냅숏 길(글자로 떴다 다시 쓰기)도 같다. 처음 판에서 `[]` 가 깨졌다 - 파서가 따옴표를 벗긴 뒤 빈 시퀀스로 읽었다(D-206 (b)).
     - 복사(`FromText`)는 새 칸을 받고, 원본을 바꿔도 사본은 그대로다. 오브젝트를 부수면 칸이 돌아오고 캔버스를 내리면 나머지도 돌아온다.
     전체 솔루션(`Debug_Game2D`)·에디터 호스트(`Debug`)·테스트 전부가 경고 없이 서고 통과한다(다른 세션의 테스트가 함께 돌면 에디터의 마우스
     테스트 하나가 흔들렸다 - 혼자 돌린 판은 통과).
     **뮤테이션 27/27 잡힘**(`tools/mutations-text2.txt`, 변이마다 실패 메시지를 확인했다). 처음 판에서 하나가 살았다: 칸 사이 틈을 없애는 변이 -
     테스트가 겹치지 않는 것만 봤다. 이웃 칸 사이 1 px 틈 검사를 더해 잡았다. 테스트를 쓰는 동안 세 자리를 더 채웠다(옵션이 바뀐 뒤의 재레이아웃,
     떼인 텍스트의 캐시, `Clip` 의 조각 자르기) - 처음 목록이 그 셋을 재지 않았다.
3. **에디터.** 인스펙터 글자 칸, 캔버스 뷰 선택·외곽선, 폰트 임포트 옵션, 폰트 없음 경고.
   - 완료 조건: 실제 에디터 창에서 텍스트 오브젝트를 만들어 글자를 치면 커맨드 하나이고 되돌리면 옛 글자다. 캔버스 뷰에서 글자 위를 누르면 그 오브젝트가 골라진다.
4. **SDF 와 외곽선.** 렌더러 `shading` 갈래, SDF 셰이더(세 백엔드 바이트코드), 임포트 옵션 `renderMode: Sdf`.
   - 완료 조건: 외곽선 폭을 퍼짐보다 크게 줘도 사각형이 칠해지지 않는다(§1.2 의 7 번 회귀 검사). 카메라를 두 배로 빼도 외곽선과 글자 굵기의 비가 같다.
     반투명 글자의 외곽선 겹침 자리가 진해지지 않는다.
5. **뒤의 것** `[열림]`: `prewarm`, 부분 영역 업로드와 R8 포맷, 자동 크기, 리치 텍스트와 패밀리, 로컬라이징 키, 화면 공간 UI 텍스트, 3D 텍스트, 비동기 래스터화, `.jpak` 에 미리 뜬 아틀라스.

## 6. 결정 (2026-09-25 확인, D-200)

1. **문자열 자리**: §3.1 의 (a). 컴포넌트는 `TextId`(번호 + 세대)만 들고, 글자는 호스트 소유 `TextStore` 가 든다. D-51 에 예외를 두지 않는다.
2. **라이브러리**: 1 판은 stb_truetype(`ThirdParty/stb/stb_truetype.h` 정식 사본). 커널 표면을 라이브러리와 무관하게 닫아, 작은 한글 선명도나
   복잡한 문자 체계가 필요해지면 FreeType + HarfBuzz 로 커널 안만 바꾼다.
3. **새로 두는 것**: Tier E 커널 모듈 `JBroText`, 컴포넌트 `Text2D`, 서비스 `Text2DService`, 에셋 타입 `Font`, 저장소 `TextStore`.
4. **렌더 모드**: 1 판은 `Bitmap`(지금 스프라이트 경로, 렌더러 변경 없음). `Sdf` 와 외곽선은 4 단계.
5. **줄바꿈 기본값**: `Word`(어절). 초안에는 이 질문의 권고가 없었으므로 확인 때 정했다 - 한국어 웹 문서가 `word-break: keep-all` 로
   어절 단위를 고르는 것이 흔하고, 음절 단위는 `wrapMode: Character` 로 고를 수 있다. 어절이 상자보다 길면 글자에서 끊는다(§3.8).
   필드 기본값 하나라 뒤집는 비용이 작다.
6. **패밀리·스타일**: 1 판에서 뺀다. `Font` 에셋 하나 = face 하나, 폴백은 프로젝트의 순서 있는 폰트 목록 하나(§3.7).

## 7. 열린 것과 가정 모음

- `[가정]` FreeType SDF 기본 퍼짐 8 px(§1.2 의 7 번). 수치는 결론(상한이 글자 크기에 묶인다)에 영향이 없다.
- `[가정]` 64 px 음절 칸 크기와 페이지당 글자 수(§1.2 의 8 번), KS X 1001 미리 채우기의 페이지 수(§3.6)는 계산이고 재지 않았다. 2 단계에서 잰다.
- `[완료]` ~~stb_truetype 의 GPOS 쌍 조정이 시험 폰트의 커닝을 읽는다~~ - 읽는다(1 단계, `AV` -15·`To` -74 가 fontTools 와 같다).
  원본 Noto Sans KR 의 `kern` 기능도 조회 형식 2(부표 형식 1·2, 값 형식 XAdvance)이고 확장 조회로 감싸지 않았다(2026-09-25 fontTools 로 확인).
- `[열림]` stb 는 GPOS 의 **확장 조회(형식 9)** 와 XAdvance 가 아닌 값 형식을 건너뛰고(`stb_truetype.h:2522·2539`), 그때 커닝이 **조용히 0** 이다.
  사용자 폰트가 그런 모양이면 커닝이 사라진다. 폰트 임포트(2 단계)에서 GPOS 모양을 읽어 경고할지, FreeType + HarfBuzz 로 갈지(D-200 (2)) 그때 판단한다.
- `[열림]` 1 단계에서 하지 않은 것: 결합 문자(U+0301 등)의 위치 잡기(GPOS mark), 탭 멈춤 자리, 줄 머리·꼬리 금칙(닫는 괄호로 줄을 시작하지 않기 등),
  옛한글 자모 조합. (글리프를 반만 자르는 `Clip` 은 2 단계에서 섰다.)
- `[열림]` 2 단계에서 하지 않은 것:
  - **프로젝트 기본 폰트와 폴백 목록**(D-200 (6)). 지금은 `fontId` 가 비면 그리지 않고 경고한다. `.jproject` 에 자리가 필요하다 - 3 단계(에디터의
    프로젝트 설정)와 함께 한다.
  - `pixelSnap`. 필드를 두지 않았다.
  - 한 텍스트의 글리프끼리의 그리기 순서. 정렬 키의 예약 16 비트를 아직 쓰지 않는다 - 비트맵 글리프는 거의 겹치지 않아 보이지 않지만, 4 단계의
    외곽선은 이웃에 겹치므로 그때 글리프 순번을 넣는다(§3.3).
  - 스크립트 DLL 안의 `TextStore` 는 묶지 않았다(`Bind` 를 부르는 곳이 없다). 스크립트는 서비스만 쓰므로 지금은 문제가 없지만, 사용자 스크립트의
    리플렉션 필드가 `TextId` 를 가지면 DLL 쪽 코덱이 DLL 의 빈 저장소를 본다. 그런 필드가 생길 때 `ScriptModuleLoadContext` 로 묶는다.
  - 인스펙터는 `TextId` 를 이스케이프한 한 줄 칸(512 바이트)으로 보인다. 여러 줄 입력 칸은 3 단계다.
  - 글자 크기가 매 프레임 조금씩 바뀌는 연출(크기 트윈)은 정수 픽셀마다 새 글리프를 뜬다 - 아틀라스가 크기 수만큼 자란다. SDF(4 단계)가 답이다.
- `[열림]` 전체 페이지 업로드 + GPU 대기의 실제 비용. 한 글자씩 찍는 연출에서 끊기면 부분 영역 쓰기를 RHI 에 연다.
- `[열림]` `RenderWorld2D` 예약 용량에 글자 수를 어떻게 넣나(§3.3).
- `[열림]` 오래 안 쓴 글리프의 퇴출. 1 판은 폰트 재로드 때만 비운다.
- `[열림]` todo-2d 가 텍스트의 선행 조건으로 적은 렌더 패스 그래프는 **비트맵 텍스트에는 필요 없다**(§3.3·§3.5). SDF 도 패스를 늘리지 않으므로
  필요 없다고 본다. 그래프가 필요한 것은 2D 라이팅·후처리다.
