Modules\JBroEditor\Source\Panel\InspectorPanel.cpp	multi:only-the-primary-is-edited	        const Array<GameObject*> chosen = m_editor->GetTopLevelSelectedObjects();	        Array<GameObject*> chosen;
Modules\JBroEditor\Source\Panel\InspectorPanel.cpp	multi:a-chosen-child-is-a-target-too	        const Array<GameObject*> chosen = m_editor->GetTopLevelSelectedObjects();	        const Array<GameObject*> chosen = m_editor->GetSelectedObjects();
Modules\JBroEditor\Source\Panel\InspectorPanel.cpp	multi:numbers-copied-instead-of-offset	        ScalarRun editedRun;\n        const bool numeric = CollectScalarRun(type, address, editedRun)\n            || SameName(type.typeName, "float");	        ScalarRun editedRun;\n        const bool numeric = false;
Modules\JBroEditor\Source\Panel\InspectorPanel.cpp	multi:delta-measured-before-the-revert	            for (std::uint32_t at = 0; at < deltaCount && at < beforeRun.count; ++at)\n            {\n                delta[at] -= *beforeRun.values[at];\n            }	            for (std::uint32_t at = 0; at < deltaCount && at < beforeRun.count; ++at)\n            {\n                (void)at;\n            }
Modules\JBroEditor\Source\Panel\InspectorPanel.cpp	multi:ordinal-ignored-when-matching	            if (ComponentBase* found =\n                FindComponentAt(*chosen[index], context.typeId, ordinal))	            if (ComponentBase* found =\n                FindComponentAt(*chosen[index], context.typeId, 0))
Modules\JBroEditor\Source\Command\CompoundCommand.cpp	compound:a-failed-part-leaves-the-rest-applied	            for (std::size_t done = index; done > 0; --done)\n            {\n                m_commands[done - 1]->Undo();\n            }	            for (std::size_t done = index; done > 0; --done)\n            {\n                (void)done;\n            }
Modules\JBroEditor\Source\Command\CompoundCommand.cpp	compound:an-empty-one-succeeds	        if (m_commands.IsEmpty())\n        {\n            // 묶을 것이 없다. 빈 것을 스택에 올리면 Ctrl+Z 가 헛걸음한다.\n            return false;\n        }	        if (false)\n        {\n            return false;\n        }
Modules\JBroEditor\Source\Command\CompoundCommand.cpp	compound:undo-in-order-not-reverse	        for (std::size_t index = m_commands.Size(); index > 0; --index)\n        {\n            m_commands[index - 1]->Undo();\n        }	        for (std::size_t index = 0; index < m_commands.Size(); ++index)\n        {\n            m_commands[index]->Undo();\n        }
Modules\JBroEditor\Source\Command\CompoundCommand.cpp	compound:merges-across-different-sizes	        if (other == nullptr || other->m_commands.Size() != m_commands.Size())	        if (other == nullptr)
Modules\JBroEditor\Source\Command\CompoundCommand.cpp	compound:merges-even-when-a-child-refuses	            if (false == m_commands[index]->CanMerge(*other->m_commands[index]))\n            {\n                return false;\n            }	            if (false)\n            {\n                return false;\n            }
